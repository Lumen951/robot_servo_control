# 差速驱动轮电机系统——详细实现思路

> 题目：A 系统——差速驱动轮电机系统（50 kg 移动底盘，48 V，150 W/轮）

---

## 一、任务分析与负载计算

### 1.1 给定参数

| 参数 | 数值 | 说明 |
|------|------|------|
| 整机质量 m | 50 kg | 两侧独立驱动，平均分担牵引力 |
| 驱动轮半径 r | 0.08 m | 忽略轮胎变形 |
| 滚动阻力系数 μ | 0.04 | 需计平路和坡道 |
| 最大坡度 θ | 5° | 上行/下行制动 |
| 机械传动效率 η | 0.90 | 减速机构自行选取 |
| 低速靠站轮速 | ±(60+AB) rpm | 精确定位 |
| 通道巡航轮速 | ±(240+CD) rpm | 高速搬运 |
| 直流母线电压 | 48 V | 电池供电 |
| 单轮电机连续功率上限 | 150 W | 10 s 内允许 2 倍过载 |

### 1.2 负载力分析

**平路工况（θ = 0°）：**
- 滚动阻力：$F_r = \mu \cdot m \cdot g = 0.04 \times 50 \times 9.8 = 19.6 \, \text{N}$
- 每轮分担：$F_{r,w} = 9.8 \, \text{N/轮}$

**坡道工况（θ = 5°，最恶劣工况）：**
- 滚动阻力：$F_r = \mu \cdot m \cdot g \cdot \cos 5° = 0.04 \times 50 \times 9.8 \times 0.996 = 19.5 \, \text{N}$
- 坡道阻力：$F_s = m \cdot g \cdot \sin 5° = 50 \times 9.8 \times 0.0872 = 42.7 \, \text{N}$
- 总阻力：$F_{total} = 19.5 + 42.7 = 62.2 \, \text{N}$
- **每轮分担：$F_{w} = 31.1 \, \text{N/轮}$**

**每轮轮端转矩：**
- 坡道稳态：$T_w = F_w \cdot r = 31.1 \times 0.08 = 2.49 \, \text{Nm/轮}$
- 平路稳态：$T_{w,flat} = 9.8 \times 0.08 = 0.784 \, \text{Nm/轮}$

### 1.3 加速过程转矩计算

**要求：0 → 最高指令速度，上升时间 ≤ 0.8 s，在最大坡度工况下。**

- 最高轮速：$n_{max} = 240 + CD \, \text{rpm}$（以 240 rpm 估算）
- 轮端角速度：$\omega_w = 240 \times 2\pi / 60 = 25.13 \, \text{rad/s}$
- 所需角加速度：$\alpha = \omega_w / t_r = 25.13 / 0.8 = 31.4 \, \text{rad/s}^2$
- 整车等效到单轮的转动惯量：$J_{eq} = \frac{1}{2} \cdot m \cdot r^2 = \frac{1}{2} \times 50 \times 0.08^2 = 0.16 \, \text{kg·m}^2$
- 加速转矩（单轮）：$T_{acc} = J_{eq} \cdot \alpha = 0.16 \times 31.4 = 5.03 \, \text{Nm}$
- **总转矩需求（坡道加速最恶劣工况）：**
  $$T_{w,max} = T_{acc} + T_w = 5.03 + 2.49 = 7.52 \, \text{Nm/轮}$$

### 1.4 功率校核

- 巡航线速度：$v = \omega_w \cdot r = 25.13 \times 0.08 = 2.01 \, \text{m/s}$
- 巡航功率（坡道，单轮）：$P_{cruise} = F_w \cdot v = 31.1 \times 2.01 = 62.5 \, \text{W/轮}$
- 考虑传动效率：$P_{motor,cruise} = 62.5 / 0.90 = 69.4 \, \text{W} < 150 \, \text{W}$ ✓
- 加速功率（峰值）：$P_{peak} = T_{w,max} \cdot \omega_w / \eta = 7.52 \times 25.13 / 0.90 = 210 \, \text{W}$
- 允许短时 2× 过载：$150 \times 2 = 300 \, \text{W} > 210 \, \text{W}$ ✓

**结论：150 W 连续功率 + 2× 短时过载满足全部工况需求。**

---

## 二、电机与减速器选型

### 2.1 减速器选型

选择**单级行星齿轮减速器**，理由：结构紧凑、效率高（η = 0.90~0.95）、回差小。

减速比选择原则：使电机工作在高效转速区间（2000~4000 rpm）。
- 巡航轮速 240+CD rpm，若减速比 i = 10:1，则电机转速 ≈ 2400+ rpm → 合理
- **推荐减速比 i = 10:1**

减速器到电机端的折算：
- 电机端转矩需求：$T_m = T_{w,max} / (i \cdot \eta) = 7.52 / (10 \times 0.90) = 0.836 \, \text{Nm}$
- 电机额定转速需求：$n_m = (240+CD) \times 10 = 2400+ \, \text{rpm}$

### 2.2 电机选型

**方案对比**：

| 方案 | 电机类型 | 优势 | 劣势 | 适合度 |
|------|----------|------|------|--------|
| A | 直流有刷电机 | 控制最简单，H 桥即可，成本低 | 有刷磨损，效率略低 | ★★★★ |
| B | BLDC（无刷直流） | 效率高，寿命长，扭矩密度大 | 需三相逆变 + 换相逻辑 | ★★★★★ |
| C | PMSM（永磁同步） | 最高效率，控制精度最高 | 需 FOC 算法，成本高 | ★★★ |
| D | 感应电机 | 可靠性高 | 效率低，体积大，不适合低压场景 | ★★ |

**选型结论：方案 B——BLDC 电机**

理由：
- 48V 低压直流供电场景下，BLDC 兼顾高效率、长寿命和高扭矩密度
- 目前主流移动机器人（AGV、四足机器人等）均采用 BLDC 方案
- 内置霍尔传感器和编码器，省去外置测速装置，降低系统复杂度

选型目标参数：
- 额定电压 48 V，额定转速 ≥ 2400 rpm，额定转矩 ≥ 0.5 Nm，峰值转矩 ≥ 1.0 Nm
- 内置编码器（速度反馈），内置霍尔传感器（换相 + 电流反馈）

**备选方案说明：方案 A（有刷直流 + H 桥）**
- 有刷直流电机方案同样可行：控制更简单，仅需 H 桥（4 个 MOSFET）即可实现四象限运行
- H 桥双极性 PWM 调制：VT1+VT4 同时导通时正转电动/反转制动，VT2+VT3 同时导通时反转电动/正转制动
- 可选用直流有刷电机配合测速发电机实现转速反馈
- 优势是控制电路简单，劣势是电刷磨损影响寿命，效率略低于 BLDC
- 若追求低成本和快速验证，有刷方案是合理选择

### 2.3 电机参数折算（用于控制设计）

选定电机后需计算以下参数（以 BLDC 线性化等效模型为例）：

| 参数 | 符号 | 来源 |
|------|------|------|
| 电枢电阻 | R | 电机数据手册 |
| 电枢电感 | L | 电机数据手册 |
| 反电动势常数 | $K_e$ | $K_e = V_{rated} / \omega_{rated}$ |
| 转矩常数 | $K_t$ | = $K_e$（SI 单位制） |
| 等效转动惯量（折算到电机轴） | $J_{eq}$ | $J_{motor} + J_{robot}/(i^2 \cdot \eta)$ |
| 电磁时间常数 | $T_l = L/R$ | 电枢回路 |
| 机电时间常数 | $T_m = J_{eq} R / (K_e K_t)$ | 机械系统 |

---

## 三、驱动电路设计

### 3.1 功率驱动方案

BLDC 电机需要**三相全桥逆变器**（有刷方案则用 H 桥，此处以 BLDC 方案为主）：

```
        VT1        VT3        VT5
48V ────┤──── A ────┤──── B ────┤
        │          │          │
       VT2        VT4        VT6
        │          │          │
GND ────┴──────────┴──────────┘
```

- **VT1~VT6**：N 沟道功率 MOSFET（48V 系统选耐压 ≥ 100V 的器件）
  - 选型依据：额定电流 > 电机峰值电流 × 2，导通电阻低，开关速度快
  - 可选用半桥驱动 IC（如 BTS724G）或分立 MOSFET
- **驱动方式**：六步换相法（BLDC 经典驱动方式）
  - 通过检测转子位置（霍尔传感器），按固定顺序切换三相桥臂通断
  - 每个电周期 6 种状态，两两导通方式
  - BLDC 电磁转矩与电流成正比，控制方法与直流电机类似
- **PWM 调制**：对导通相的下桥臂施加 PWM 斩波，控制等效电压
  - 斩波频率：10~20 kHz
  - 死区时间：0.5~1 μs（防止上下桥臂直通）

### 3.2 电流检测

- **方案**：霍尔电流传感器，串联在母线（直流侧）采样
- 常用型号：CSM006NPT（量程 0~19.2A）或 ACS712 20A（灵敏度 100mV/A）
- 反馈系数标定：$\beta = V_{ref} / I_{max}$
- 需加低通滤波器滤除 PWM 开关纹波
- 测量电路设计要点：
  1. 连接电压跟随器提高带载能力
  2. 使用加减法电路将零位输出电压设定为 0
  3. 使用比例运算电路将反馈系数设定为目标值

### 3.3 转速/位置检测

- BLDC 电机内置**霍尔位置传感器**（3 个，间隔 120°电角度），可同时用于：
  - 换相逻辑判定
  - 转速计算（通过霍尔信号频率）
- 如需更高精度（低速靠站定位 ±(60+AB) rpm），可增加**增量式编码器**
- 反馈系数标定：$\alpha = V_{ref} / n_{max}$

### 3.4 保护电路

- **过流保护**：利用电流传感器反馈 + 硬件比较器，电流超阈值立即关断 MOSFET
- **过热保护**：在电机绕组附近安装热保护器（如 17AM023A5），检测温度超阈值时断开
- **过压/欠压保护**：48V 母线电压检测，超范围报警停机

---

## 四、控制策略设计——转速电流双闭环

### 4.1 系统总体控制结构

```
[转速给定 ±n*] ──→ [给定滤波] ──⊕──→ [ASR(PI)] ──→ [限幅] ──→ [ACR(PI)] ──→ [限幅] ──→ [PWM调制]
                                      ↑ (-)                              │
                                 [转速滤波]                              │
                                      ↑                                  ↓
                               [霍尔/编码器]                    [PWM调制+死区]
                                      ↑                                  │
                               [BLDC+减速器+车轮] ←── [电流滤波] ←── [电流传感器]
                                      ↑
                              [负载：摩擦+坡道+惯性]
```

各模块功能：
- **ASR（转速调节器）**：消除转速静差，抗负载扰动，输出电流给定信号
- **ACR（电流调节器）**：快速电流跟随，限流保护电机和 MOSFET
- **PWM + 三相桥**：六步换相驱动 BLDC，占空比控制等效电压
- **负载模型**：含滚动摩擦、坡道力、整车惯性

**BLDC 等效处理**：BLDC 在两两导通方式下，从逆变器直流侧看等效为两相绕组串联，电磁转矩与电流成正比，因此可将电流环等效为直流电机模型进行设计。

### 4.2 电流环 ACR 设计（典型 I 型系统）

**Step 1：确定时间常数**
- PWM 斩波滞后时间常数：$T_s$（与开关频率有关）
- 电流滤波时间常数：$T_{oi}$（滤平 PWM 纹波，取 $T_{oi} = 1\sim3 \, \text{ms}$）
- 电磁时间常数：$T_l = L / R$
- 小惯性环节合并：$T_{\Sigma i} = T_s + T_{oi}$

**Step 2：ACR 结构选择**
- 控制对象含一个大惯性环节（$T_l$）和一个小惯性群（$T_{\Sigma i}$）
- 因为 $T_l \gg T_{\Sigma i}$，按**典型 I 型系统**设计
- ACR 选 **PI 调节器**：$W_{ACR}(s) = K_i \frac{\tau_i s + 1}{\tau_i s}$
- 零点对消大惯性极点：$\tau_i = T_l$

**Step 3：参数计算**
- 按电流超调 ≤ 5% 的要求，查课本表取阻尼比 ζ 对应的 $KT$ 值
- 比例系数：$K_i = \frac{\tau_i}{2 K_s \beta T_{\Sigma i}} \cdot \frac{1}{KT}$（$K_s$ 为 PWM 放大倍数，$\beta$ 为电流反馈系数）

**Step 4：校验近似条件**
- PWM 传递函数近似条件：$\omega_{ci} \leq \frac{1}{3T_s}$
- 小惯性环节合并条件：$\omega_{ci} \leq \frac{1}{3}\sqrt{\frac{1}{T_s T_{oi}}}$

**输出限幅**：ACR 限幅 = 最大 PWM 占空比对应的电压（如 ±10V）

### 4.3 转速环 ASR 设计（典型 II 型系统）

**Step 1：电流环闭环等效**
- 电流环闭环传递函数简化为一阶惯性：$W_{ci}(s) \approx \frac{1}{\beta(2 T_{\Sigma i} s + 1)}$
- 等效时间常数：$T_{eqi} = 2 T_{\Sigma i}$

**Step 2：确定时间常数**
- 转速滤波时间常数：$T_{on}$（取决于测速装置，取 5~10 ms）
- 小惯性群合并：$T_{\Sigma n} = T_{eqi} + T_{on}$

**Step 3：ASR 结构选择**
- 转速环要求无静差 → 需要积分环节 → 按**典型 II 型系统**设计
- 从静态无差和动态性能两个角度综合考虑，典型 II 型系统具有较好的抗扰性能
- ASR 选 **PI 调节器**：$W_{ASR}(s) = K_n \frac{\tau_n s + 1}{\tau_n s}$

**Step 4：参数计算**
- 按跟随性能和抗扰性能最优原则，取中频宽 $h = 5$
- 超前时间常数：$\tau_n = h \cdot T_{\Sigma n} = 5 T_{\Sigma n}$
- 转速开环增益：$K_N = \frac{h+1}{2 h^2 T_{\Sigma n}^2}$
- 比例系数：$K_n = \frac{(h+1) \beta}{2 h \alpha T_{\Sigma n}} \cdot J_{eq}$（$\alpha$ 为转速反馈系数）

**Step 5：校验**
- 转速超调量：$\sigma_n < 10\%$（考虑 ASR 饱和非线性后的实际超调）
- 电流环简化条件：$\omega_{cn} \leq \frac{1}{3T_{eqi}}$
- 小惯性合并条件：$\omega_{cn} \leq \frac{1}{3}\sqrt{\frac{1}{T_{eqi} T_{on}}}$

**输出限幅**：ASR 限幅 = 电流给定最大值（对应电机峰值电流）

### 4.4 积分饱和问题与改进

**问题发现**：
- 仅对 PI 控制器的输出做限幅时，积分器会在误差为正时持续积累
- 当误差变号后，积分器由于积累过多无法及时退饱和，导致控制出现巨大迟滞
- 这会引起转速超调和后续震荡

**改进方案：双限幅（积分限幅 + 输出限幅）**
- 在 PI 控制器中同时增加积分器限幅电路
- 保证积分器和 PI 输出在给定值变号时同时退饱和
- 效果：转速超调从约 5.6% 降至 2.2%，退饱和延迟从 0.32s 降至接近 0

### 4.5 特殊工况控制策略

| 工况 | 控制策略 | 说明 |
|------|----------|------|
| **正转启动** | ASR 给定阶跃 +n*，电流环恒流加速 | 标准双闭环启动过程 |
| **反转切换** | ASR 给定从 +n* 跳变到 -n* | 转速先制动归零再反向启动，仿真验证约 0.4~0.5s 完成 |
| **坡道下行制动** | 电机进入再生制动状态，能量回馈母线 | 需在 48V 母线上增加制动斩波器 + 制动电阻防过压；或设计能量回收电路 |
| **紧急停车** | ASR 给定突变到 0，电流环施加最大制动电流 | 快速制动至 0；坡道工况需维持制动电流防止溜坡 |
| **低速靠站** | ASR 给定为低转速 ±(60+AB) rpm | PI 无静差特性保证低速段仍有良好调速精度 |

**坡道下行制动是本设计的重点难点**：
- 下坡时重力分量与运动方向相同，电机进入再生制动模式
- 三相桥在制动时自动将电能回馈到直流母线
- 若无制动电阻或能量回收电路，母线电压将持续升高直至损坏器件
- 需要设计制动斩波电路：母线电压超过阈值时接通制动电阻消耗多余能量

---

## 五、仿真验证方案

### 5.1 Simulink 模型搭建

```
模型结构：
├── 转速给定信号（Step / Signal Builder，模拟正反转、停车等）
├── ASR（PI + 双限幅）
├── ACR（PI + 输出限幅）
├── PWM 发生器（等效为比例环节 Ks + 延迟 Ts）
├── BLDC 等效模型（电枢回路 + 反电动势 + 机械负载）
│   ├── 电枢方程：U = R·I + L·dI/dt + E
│   ├── 反电动势：E = Ke · n
│   └── 机械方程：T - T_L = J · dω/dt
├── 负载模型
│   ├── 平路：T_L = μ·m·g·r / (i·η)（恒定）
│   ├── 坡道上行：T_L = (μ·m·g·cosθ + m·g·sinθ)·r / (i·η)
│   └── 坡道下行：T_L = (-m·g·sinθ + μ·m·g·cosθ)·r / (i·η)（负向负载）
├── 电流反馈滤波（低通，Toi）
└── 转速反馈滤波（低通，Ton）
```

仿真步长：0.0001 s（保证 PWM 调制精度和转速过零点检测）

### 5.2 验证项目

| 序号 | 验证工况 | 关注指标 | 达标标准 |
|------|----------|----------|----------|
| 1 | 平路 0→巡航转速启动 | 上升时间、转速超调、电流超调 | $t_r \leq 0.8$ s, $\sigma_n < 10\%$, $\sigma_i < 5\%$ |
| 2 | 坡道 0→巡航转速启动（最恶劣工况） | 上升时间、超调 | 同上 |
| 3 | 正转→反转切换 | 反转完成时间、超调 | 切换平稳 |
| 4 | 坡道下行制动 | 制动效果、母线电压 | 不溜坡，母线不过压 |
| 5 | 紧急停车 | 制动时间 | 尽快停车 |
| 6 | 低速靠站运行 | 稳态精度 | 转速波动小 |

### 5.3 预期结果

- **启动过程**：电流环恒流加速 → 转速线性上升 → ASR 退饱和 → 转速略超调 → 稳定
- **预期指标**：本场景负载较轻（电机端 0.836 Nm），预期上升时间 < 0.5s，超调 < 5%
- **反转过程**：转速先线性下降（制动时摩擦力矩和电磁力矩同向），过零后线性上升至反向稳态（电动时摩擦力矩和电磁力矩反向，转速变化率更小）

---

## 六、效率与成本分析

### 6.1 效率分析

主要损耗环节：

| 环节 | 损耗机制 | 估算 |
|------|----------|------|
| BLDC 电机铜耗 | $I^2 R$ | 由电机参数决定 |
| BLDC 电机铁耗 | 涡流 + 磁滞 | 与转速成正比 |
| MOSFET 导通损耗 | $I^2 R_{ds(on)}$ | mΩ 级，较小 |
| MOSFET 开关损耗 | 开关频率 × 电压 × 电流 | 10~20 kHz 时约 0.1~0.5 W |
| 减速器摩擦损耗 | 齿轮啮合 | 已计入 η = 0.90 |
| 控制电路 | 运放、传感器供电 | < 1 W |

**总效率估算**：电机效率（~80%）× 减速器效率（90%）× 逆变器效率（~95%）≈ **68%**

### 6.2 成本估算（BOM）

| 器件 | 参考型号 | 预估单价（元） | 数量 | 小计 |
|------|----------|----------------|------|------|
| BLDC 电机 | 48V/150W 级 | 500~800 | 2 | 1000~1600 |
| 行星减速器 | 10:1 | 100~200 | 2 | 200~400 |
| MOSFET 三相桥模块 | 半桥驱动 IC 或分立 | 30~50 | 2 | 60~100 |
| 电流传感器 | 霍尔传感器 | 6~30 | 2 | 12~60 |
| 编码器（如需外置） | 增量式 1000~2000 线 | 50~100 | 2 | 100~200 |
| 控制器 | MCU/DSP | 20~50 | 1 | 20~50 |
| 无源器件 | 电阻、电容、PCB | ~20 | 1 | 20 |
| **双轮系统合计** | | | | **~1500~2500 元** |

---

## 七、实现路线图

```
第1步：负载计算 → 确认 150W/轮 满足需求
    ↓
第2步：电机+减速器选型 → 确定电机参数（R, L, Ke, J）
    ↓
第3步：驱动电路设计 → 三相逆变桥 + 电流/转速传感器
    ↓
第4步：双闭环 PI 参数计算 → 先电流环（I型）再转速环（II型）
    ↓
第5步：Simulink 仿真 → 6 种工况验证
    ↓
第6步：积分饱和改进 → 双限幅
    ↓
第7步：坡道下行制动设计 → 制动电阻/能量回收
    ↓
第8步：效率 + 成本分析 → 输出 BOM
    ↓
第9步：撰写报告 + 制作 PPT
```

---

## 八、待进一步解决的问题

| 序号 | 问题 | 说明 |
|------|------|------|
| 1 | BLDC 六步换相的转矩脉动 | 六步换相存在换相转矩脉动，低速时更明显，可能影响靠站精度；可考虑改用 SVPWM 正弦驱动 |
| 2 | 坡道下行制动的能量回收 | 目前方案仅考虑制动电阻耗散，未设计能量回馈电池的电路，后续可加入 DC-DC 双向变换器 |
| 3 | 双轮差速协调控制 | 本设计聚焦单轮驱动，未涉及左右轮差速转向的协调控制策略 |
| 4 | PI 参数在线自适应 | 当前 PI 参数为离线整定，负载变化（空载/满载/坡道）时可能不是最优；可引入自适应或模糊 PI |
| 5 | 实物验证与仿真差异 | 仿真中未考虑电机非线性（磁饱和、齿槽转矩）、传感器噪声、温度漂移等因素 |
| 6 | 低速靠站定位精度 | 低速段编码器分辨率有限，霍尔传感器测速精度不足，可能需要更高分辨率编码器或观测器算法 |
| 7 | 紧急制动时的防溜坡策略 | 坡道紧急停车后需维持制动力矩，断电后如何保持（机械刹车或电磁抱闸）需进一步设计 |
